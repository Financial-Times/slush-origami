{
	"private": true,
	"devDependencies": {
<% if (useBabel) { %>		"babel-preset-latest": "^6.22.0",
		"babelify": "^7.3.0",
<% } %>		"browserify": "^13.1.1",
		"mocha": "^3.2.0",
		"chai": "^3.5.0",
		"mochify": "^2.19.0",
		"watchify": "^3.7.0"
	},
	"scripts": {
		"start": "mochify --watch",
		"phantom": "mochify",
		"wd": "mochify --wd",
		"cover": "mochify --cover",
		"test": "npm run phantom && npm run wd && npm run cover"
	}
}
